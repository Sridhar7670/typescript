// We explicitly import what we need from the mathUtils module.
import { add, subtract } from './mathsUtils';

const sum = add(5, 3);
const difference = subtract(10, 4);

console.log(sum);
console.log(difference);